const init = () =>{
  // const imageList = document.querySelectorAll('.item_grid_works');
  // // console.log(imageList);
  // for(let image of imageList){
  //   console.log(image.style.backgroundImage.offsetWidth );
  // }
}

export default {init};